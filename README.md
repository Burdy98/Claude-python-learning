# Claude-python-learning

Me learning python though getting Claude to write me purposefully vulnerably 'server.py' then me writing scripts to evuntally obtain RCE via python only.

Each folder will contain 2 files, `server.py` and `solution.py` the server file will be the file server created by claude and the solution.py will be the response coded by me.

No hints, no spoilers from Claude while I'm working on one — each challenge has its own `README.md` with a spoiler-tagged writeup I only fill in / reveal once I've actually got RCE.

## Challenges

- [`vulnbank/`](vulnbank/README.md) — raw TCP + newline-delimited JSON server
- [`pyshop/`](pyshop/README.md) — HTTP + JSON API server
